{
  "name": "tortopani-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static marketing site for TORTOPANI baking school (no build step). Dev-only test tooling.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
